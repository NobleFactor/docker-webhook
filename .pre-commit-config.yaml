repos:
  - repo: local
    hooks:
      - id: shell-format-and-lint
        name: Shell format & lint (shfmt + shellcheck)
        entry: bash -c "make Test-ShellScripts"
        language: system
        files: '^(bin|test|webhook.config)/.*'
        stages: [commit]